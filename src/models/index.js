const User = require('./user');
const Player = require('./player');

module.exports = { User, Player };                                // in ES6 when property and variable have the same name you can shorten it ex. User: User
